<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cl_dialog_add_image"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="@dimen/_12sdp"
    android:background="@drawable/background_dialog">

    <TextView
        android:id="@+id/tv_dialog_add_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/_8sdp"
        android:layout_marginTop="@dimen/_15sdp"
        android:layout_marginEnd="@dimen/_8sdp"
        android:background="@null"
        android:drawablePadding="@dimen/_15sdp"
        android:gravity="center_vertical"
        android:importantForAutofill="no"
        android:includeFontPadding="false"
        android:paddingBottom="@dimen/_15sdp"
        android:text="@string/gallery"
        android:textColor="?attr/color"
        android:textSize="@dimen/_17ssp"
        android:textStyle="normal"
        app:drawableStartCompat="@drawable/ic_image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_goneMarginEnd="@dimen/_6sdp" />

    <TextView
        android:id="@+id/tv_dialog_add_photo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/_8sdp"
        android:layout_marginEnd="@dimen/_8sdp"
        android:background="@null"
        android:drawablePadding="@dimen/_15sdp"
        android:gravity="center_vertical"
        android:importantForAutofill="no"
        android:includeFontPadding="false"
        android:paddingBottom="@dimen/_15sdp"
        android:text="@string/camera"
        android:textColor="?attr/color"
        android:textSize="@dimen/_17ssp"
        android:textStyle="normal"
        app:drawableStartCompat="@drawable/ic_camera"
        app:layout_constraintTop_toBottomOf="@+id/tv_dialog_add_image"
        app:layout_goneMarginEnd="@dimen/_6sdp" />


</androidx.constraintlayout.widget.ConstraintLayout>